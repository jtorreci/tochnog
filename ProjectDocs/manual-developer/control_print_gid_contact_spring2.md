# control_print_gid_contact_spring2 — developer

Enum + registration (INTEGER, 1 value, no_index 1, class CONTROL). no_index
chosen because the corpus spelling `print_gid_contact_spring2 1` carries the
node count as the only value (tutorial_3). db_number alias
"print_gid_contact_spring2" -> CONTROL_PRINT_GID_CONTACT_SPRING2. Parse-only:
the GNU GiD printer uses the element nodes of the model.
