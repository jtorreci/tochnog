# group_groundflow_expansion — developer

Enum + registration (DOUBLE, 1 value, class GROUNDFLOW, data_required
GROUP_TYPE). Parse-only; consumption belongs to the coupled
thermo-groundflow equations (PENDING).
