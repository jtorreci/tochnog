# post_calcul_static_pressure_height_element_group — developer

Enum + registration (INTEGER, variable DATA_ITEM_SIZE, no_index 1, class
POST, data_required POST_CALCUL_STATIC_PRESSURE_HEIGHT). Read as an int
array (db_int/db_len) inside the height-region search of
groundflow_phreatic_coord(); value -ALL accepts any node group.
