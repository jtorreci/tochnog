# group_materi_plasti_element_group — developer

Enum + registration (INTEGER, variable DATA_ITEM_SIZE, class MATERI,
data_required GROUP_TYPE). Registered in this batch; consumption PENDING:
the manual (6.698) reduces phi/phiflow/c (and the M of camclay, sigy of
tension, the deviatoric increments of hypo) by a factor (default 2./3.) for
granular elements whose nodes are shared with ("direct neighbor of") an
element of one of the listed groups. Heat_exchanger_pile_dt/_load_dt of the
corpus parse past the record (next blockers: solver singular/timeout of the
big thermo pile model + mesh concentration PENDING).
