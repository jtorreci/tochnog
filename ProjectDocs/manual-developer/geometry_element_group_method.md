# geometry_element_group_method — developer

Enum + registration (INTEGER, 1 value, class GEOMETRY, data_required
GEOMETRY_ELEMENT_GROUP). Read inside the geometry() filter (geometry.cc);
absent => -ANY default.
