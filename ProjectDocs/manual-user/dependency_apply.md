# dependency_apply

Global switch of the dependency machinery (manual Professional 6.125): when set
to `-no`, the `dependency_item`/`dependency_diagram` records are ignored;
default `-yes`. See control_dependency_apply for the per-timestep form.
